<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mffff.mapper.BrandMapper">
    <insert id="addBrand">
        insert into brand values (null, #{name}, #{company}, #{sort}, #{status})
    </insert>
    <update id="updateBrand">
        update brand set name = #{name}, status = #{status}, company = #{company}, sort = #{sort} where id = #{id}
    </update>

    <delete id="delBrand">
        delete from brand where id = #{id}
    </delete>
    <delete id="batchDelete">
        delete from brand where id in
--     这里需要注意 collection作为foreach的对象  List格式的用'list'作为键 数组对象用'array'作为键 map对象需要自定义@param这种方式
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectAll" resultType="Brand">
        select * from brand
        <where>
            <if test="brand.name != null and brand.name != ''">
                and name like #{brand.name}
            </if>
            <if test="brand.status != null">
                and status = #{brand.status}
            </if>
            <if test="brand.company != null and brand.company != ''">
                and company like #{brand.company}
            </if>
        </where>
        limit #{limit} offset #{offset}
    </select>

    <select id="selectCount" resultType="java.lang.Integer">
        select count(*) from brand
        <where>
            <if test="name != null and name != ''">
                and name like #{name}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="company != null and company != ''">
                and company like #{company}
            </if>
        </where>
    </select>
</mapper>